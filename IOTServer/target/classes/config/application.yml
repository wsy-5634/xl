server:
  port: 8081
spring:
  application:
    name: IOTServer
  datasource:
    #?useUnicode=true&amp&useSSL=false&amp&characterEncoding=UTF-8&amp&serverTimezone=GMT%2B8&amp&allowPublicKeyRetrieval=true # MySQL在高版本需要指明是否进行SSL连接 解决则加上 &useSSL=false
    url: jdbc:mysql:///fuguiot?characterEncoding=utf8&serverTimezone=UTC

    username: root
    password: root